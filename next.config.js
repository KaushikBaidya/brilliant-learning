/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["plus.unsplash.com", "res.cloudinary.com",'source.unsplash.com'],
      },
}

module.exports = nextConfig
